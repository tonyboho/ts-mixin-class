---
"ts-mixin-class": patch
---

Republishes the package with a resolved `typescript` peer dependency range. 0.0.17 shipped
`"typescript": "catalog:"` (a pnpm workspace placeholder) and cannot be installed — use
0.0.18 instead.
